<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="a">
	<!-- 자바 클래스 메소드가 호출 메소드명 = ID명 나중에 대비 -->
	<select id="memberList" resultType="memberdto">
		select * from c_member
	</select>

	<select id="oneMember" resultType="memberdto"
		parameterType="String">
		select * from c_member where memberid=#{memberid}
	</select>

	<select id="memberCount" resultType="int">
		select count(*) from
		c_member
	</select>

	<insert id="insertMember">
		insert into c_member
		values('mybatis2','육길동',1111,'010-6725-2817','mybatis2@my.com',now())
	</insert>

	<insert id="insertMemberDTO" parameterType="memberdto">
		insert into c_member
		values(#{memberid},#{name},#{pw},#{phone},#{email},now())
	</insert>

	<update id="updateMember" parameterType="memberdto">
		update c_member set
		pw=#{pw} , email=#{email} where memberid=#{memberid}
	</update>

	<delete id="deleteMember" parameterType="String">
		delete from c_member
		where memberid=#{memberid}
	</delete>

	<!-- parameterType="Stirng|int|dto" int[] limit ={0,3} -->

	<!-- <select id="memberPagingList" resultType="memberdto" parameterType="int"> 
		select * from c_member order by regtime desc limit <foreach collection="array" 
		item="num" separator=","> #{num} </foreach> </select> -->

	<select id="memberPagingList" resultType="memberdto"
		parameterType="java.util.ArrayList">
		select * from c_member order by regtime desc limit
		<foreach collection="list" item="num" separator=",">
			#{num}
		</foreach>
	</select>

	<select id="searchList" parameterType="hashmap"
		resultType="memberdto">
		select * from c_member where ${colname} like #{colvalue}
	</select>

	<select id="searchList2" parameterType="memberdto"
		resultType="memberdto">
		select * from c_member where
		<choose>
			<when test="name != null">
				name like concat('%',#{name},'%')
			</when>
			<when test="email != null">
				email like concat('%',#{email},'%')
			</when>
			<when test="phone != null">
				phone like concat('%',#{phone},'%')
			</when>
			<when test="memberid != null">
				memberid like concat('%',#{memberid},'%')
			</when>
		</choose>
		order by regtime desc
	</select>
	<sql id="membersql">
		select * from c_member
	</sql>
	<sql id="wheresql">
		where memberid=#{memberid}
	</sql>
	<select id="totalsql" resultType="memberdto" parameterType="String">
		<include refid="membersql" />
		<include refid="wheresql" />
	</select>
</mapper>